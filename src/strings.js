export function strings(a){
    return a
}

export function concatenate(b){
    return b + " " + "Now I have concatenated this string to the prior sentence."
}

export function templateLiterals(c){
    return `${c} Now I have concatenated this string to the prior sentence.`
}

